SVELTE_PUBLIC_SUPABASE_URL="your-project-url"
SVELTE_PUBLIC_SUPABASE_ANON_KEY="your-publishable-or-anon-key"

# Running the Docker image against a Supabase CLI stack on the host: point the
# URL at http://host.docker.internal:54321 and take the key from `supabase status`.
