# `testDirs` is not a recognised key -- ast-grep silently ignored it and
# `ast-grep test` discovered 0 tests while still exiting 0. The correct shape
# is testConfigs[].testDir.
ruleDirs:
  - .ast-grep/rules
testConfigs:
  - testDir: .ast-grep/tests
