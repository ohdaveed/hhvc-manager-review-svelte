pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{js,ts,svelte,json,md}"
      run: bunx prettier --write {staged_files}
      stage_fixed: true
    verify:
      run: bun run verify
    knip:
      run: bunx knip --no-exit-code
